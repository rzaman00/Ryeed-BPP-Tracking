[pytest]
testpaths = tests
# The repo is a flat checkout, not an installed package — put its root on
# sys.path so `from chasemapper import ...` resolves under a bare `pytest`
# (python -m pytest happens to work without this; CI does not use it).
pythonpath = .
